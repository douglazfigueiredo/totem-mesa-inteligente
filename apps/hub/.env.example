# Env do hub local (dev fora do Docker e dev via docker-compose.dev.yml).
# Em produção (mini-PC da loja), o env real está em deploy/hub/.env.

# Secret para autenticar a UI admin do hub (http://localhost:4000/admin).
# Gere uma vez:  openssl rand -hex 32
ADMIN_SECRET=
